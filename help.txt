Hello !
Working on my etch-a-sketch, I can't understand why my code does not work with 
a for (cell in cells)

```
const cells = grid.getElementsByClassName("cell");
for (cell in cells) {
    cell.addEventListener('mouseenter', (e) => {
        e.target.classList.add("greyed")
    })
};```


but it works when I use forEach
```
const cells = grid.getElementsByClassName("cell");
Array.from(cells).forEach((cell) => {
    cell.addEventListener('mouseenter',
        (e) => {
            e.target.classList.add("greyed")
        })
});```

I presumed both would work the same way...